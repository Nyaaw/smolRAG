# Java Sample Fixture

A minimal Maven project used as a test fixture for smolRAG end-to-end tests.

Exercises:
- Inheritance (`extends`) and interface implementation (`implements`)
- Cross-class references (method calls, field access, object creation)
- Static utility methods
- Javadoc comments on public API
- Non-Java files (properties, plain text, markdown)
